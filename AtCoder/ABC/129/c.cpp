#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

#define rep(i, N) for (int i = 0; i < N; ++i)

int main() {
    int N, M;

    vector<int> order = {0, 1};
    // step 1 or 2;
    rep(i, N) {
    }
    return 0;
}
